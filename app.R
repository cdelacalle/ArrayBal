# citar los paper de bioconductor con setrepositories() antes de colgar la aplicación a la nube.

source("/disco1/Project_Shiny/Project_Shiny/app_functions.R")

shinyApp(ui = ui, server = server)
        